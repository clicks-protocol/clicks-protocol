{
  "attestations": [
    {
      "agentId": 1,
      "value": 5000,
      "tag1": "custom",
      "tag2": "custom",
      "endpoint": "https://clicksprotocol.xyz/.well-known/agent-registration.json",
      "feedbackURI": "https://clicksprotocol.xyz/strategy/ATTESTOR-SCHEMA-V1.md",
      "note": "Bootstrap attestation — Clicks operator's first ERC-8004 feedback call. Neutral rating (0.5) because we have no evidence on Agent #1; this entry exists only to validate the Schema V1 pipeline end-to-end on Base mainnet."
    }
  ],
  "notes": [
    "Add Attestation objects here. Schema V1 requires:",
    "  value in [0, 10000] (decimals fixed at 4 → 1.0000 = perfect, 0.5000 = neutral)",
    "  tag1 in {ingest, route, split, withdraw, liquidate, custom, ''}",
    "  tag2 in {virtuals-acp, x402, direct-sdk, mcp-tool, custom, ''}",
    "  One entry per (agentId, endpoint, 24h)"
  ]
}
