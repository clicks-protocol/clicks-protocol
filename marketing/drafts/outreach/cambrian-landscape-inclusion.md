# Cambrian Network — Landscape-Inclusion DM Draft

**Targets:**
- @CambrianNetwork (X) — primary
- @0xsamgreen (X) — secondary (Mirror des Landscape-Posts)

**Channel:** X DM first, Farcaster cast as backup if ignored >7 days.

**Status:** DRAFT — not sent

**Context:** Cambrian publizierte am 21.04.26 die "Agentic Finance Landscape 2026" mit 42 Players in 4 Buckets (Trading, Prediction, Sentiment, Yield). Clicks ist nicht dabei — aber auch nicht in der falschen Kategorie, da Clicks **Settlement-Router** ist, nicht Yield-Agent.

---

## Version A — Short (X DM, ≤ 280 char)

gm — love the Agentic Finance map. One gap worth a fifth bucket: **Settlement / Treasury Infrastructure**. Clicks Protocol is live on Base (agentId 45074, ERC-8004), routes USDC 80/20 between liquid & yield. We sit *beneath* the 16 Yield Agents, not alongside. Worth a look?

---

## Version B — Follow-up (wenn sie antworten)

Thanks for engaging. Three-second pitch:

**What Clicks is:** Agent Commerce Settlement Router on Base. x402 / ACP payments come in → we split 80/20 liquid vs. yield → route into ERC-4626 vaults. We don't operate vaults ourselves.

**Why it's a new category, not "another Yield Agent":**
- The 16 Yield Agents in your map (ARMA, Mamo, Superform, …) all *allocate* capital. Clicks *routes* flow into any of them.
- A yield agent using Clicks = Clicks-routed USDC lands in their pool, automatically. We're their ingress.
- Analogy: Yield agents are vaults; Clicks is the settlement bus beneath.

**Evidence (all verifiable on-chain):**
- ERC-8004 Identity: agentId `45074` on Base ([BaseScan](https://basescan.org/token/0x8004A169FB4a3325136EB29fA0ceB6D2e539a432?a=45074))
- Live contracts (Safe multisig owned): Registry, SplitterV4, YieldRouter, FeeV2, Referral
- Schema-V1 Attestation already on-chain: Tx `0x5aec2067…`
- SDK on npm `@clicks-protocol/sdk` · MCP server · Eliza/LangChain/CrewAI plugins
- 227/227 tests green, Apache-2.0

**Ask:** Would you consider a fifth bucket in the next iteration — "Settlement / Treasury Infrastructure" (or however you'd name it)? Clicks would be the first entry; happy to help scope what else belongs there (Coinbase Agentic Wallets infra, x402 routing layers, ACP providers, …).

**Optional watch:** [clicksprotocol.xyz/org-pitch](https://raw.githubusercontent.com/clicks-protocol/clicks-protocol/feat/video-pipeline-hyperframes/video-pipeline/previews/org-pitch.gif) — 45 s walkthrough of the org architecture.

---

## Talking points if they push back

- **"Aren't you just another yield protocol?"** → We don't operate any vault. The SplitterV4 contract sends ERC-20 transfers; the YieldRouter calls `deposit()` on 3rd-party ERC-4626. Zero yield generated by us.
- **"Too niche for a category?"** → Coinbase Agentic Wallets is infra. x402 routing layers exist (CoinGecko, Browserbase already integrate). ACP providers (Virtuals). All share the "money-movement, not money-making" thesis.
- **"Proof of traction?"** → ERC-8004 live, Schema V1 first attestation on-chain, ACP Service Provider live (blocked by Virtuals Alchemy Paymaster bug, but deployed), npm downloads climbing, Cursor Directory listed.

---

## Send Plan

1. Version A via X DM to @CambrianNetwork
2. Wait 48 h. If no reply, same via @0xsamgreen (mirror poster, more likely to forward internally).
3. Wait 7 d total. If still silent, Farcaster cast tagging both.
4. Update `marketing/outreach-tracker.json` at each step.

---

## Rule-#7 Note

This doc is an LLM-drafted **advisor** artifact. Sending happens **manually by David** (or future authorized Actor). LLM does not post.
